# JBA Property Solutions

Reviewed single-page website for Railway deployment.

## Deploy

Push these files to the repository root. Railway will run `node server.js`.

## Local development

```bash
npm start
```

Then open http://localhost:3000
